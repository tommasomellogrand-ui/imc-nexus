name: Check Nexus Build 48
on:
  push:
    paths: ['.github/workflows/check-build48.yml']
permissions:
  contents: read
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check app.js syntax
        run: node --check nexus/js/app.js
