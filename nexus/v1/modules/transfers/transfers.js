(function(){"use strict";if(window.IMC_TRANSFERS)return;let s={container:null,client:null,worldId:"",clubWorldId:null,mode:"world",rows:[]};const c=v=>String(v==null?"":v).trim(),e=v=>c(v).replace(/[&<>"']/g,x=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[x]));async function load(){let q=s.client.from(s.worldId.toLowerCase()+"_gw_transfers").select("transfer_id,player_id,player_name,from_sm_world_club_id,club_from,to_sm_world_club_id,club_to,amount_text,transfer_date_text,exchange_players,imc_season,season_transfer_number,transfer_code").order("imported_at",{ascending:false});const r=await q;if(r.error)throw r.error;s.rows=r.data||[]}function row(x,label){return `<article class="tf-row"><small>${e(x.transfer_date_text||"")} ${x.imc_season?"· S"+e(x.imc_season):""}</small><strong>${e(x.player_name||x.player_id)}</strong><p>${e(x.club_from||"-")} → ${e(x.club_to||"-")}</p>${x.amount_text?`<b>${e(x.amount_text)}</b>`:""}${label?`<em>${label}</em>`:""}</article>`}function renderWorld(){s.container.innerHTML=`<section class="tf"><header><h2>Trasferimenti</h2><b>${s.rows.length}</b></header><div class="tf-list">${s.rows.map(x=>row(x,"")).join("")||'<div class="tf-empty">Nessun trasferimento.</div>'}</div></section>`}function renderClub(){const id=String(s.clubWorldId||"");const incoming=s.rows.filter(x=>String(x.to_sm_world_club_id||"")===id);const outgoing=s.rows.filter(x=>String(x.from_sm_world_club_id||"")===id);s.container.innerHTML=`<section class="tf"><header><h2>Trasferimenti</h2><b>${incoming.length+outgoing.length}</b></header><div class="tf-tabs"><button data-tf-tab="in" class="active">ENTRATE</button><button data-tf-tab="out">USCITE</button></div><div data-tf-body>${incoming.map(x=>row(x,"ENTRATA")).join("")||'<div class="tf-empty">Nessuna entrata.</div>'}</div></section>`;s.container.dataset.tfMode="in";s.container._tfIn=incoming;s.container._tfOut=outgoing}async function mount(o){if(!o||!o.container||!o.client||!o.worldId)throw Error("Transfers: parametri mancanti");s={...s,...o,worldId:c(o.worldId),mode:o.clubWorldId?"club":"world",rows:[]};s.container.innerHTML='<div class="tf-empty">Caricamento Trasferimenti…</div>';await load();s.mode==="club"?renderClub():renderWorld()}function unmount(){if(s.container){s.container.innerHTML="";delete s.container._tfIn;delete s.container._tfOut}s.container=null}document.addEventListener("click",ev=>{const b=ev.target.closest&&ev.target.closest("[data-tf-tab]");if(!b||!s.container||!s.container.contains(b))return;const tab=c(b.getAttribute("data-tf-tab"));s.container.querySelectorAll("[data-tf-tab]").forEach(x=>x.classList.toggle("active",x===b));const body=s.container.querySelector("[data-tf-body]");const list=tab==="out"?(s.container._tfOut||[]):(s.container._tfIn||[]);body.innerHTML=list.map(x=>row(x,tab==="out"?"USCITA":"ENTRATA")).join("")||`<div class="tf-empty">Nessuna ${tab==="out"?"uscita":"entrata"}.</div>`});window.IMC_TRANSFERS={mount,unmount};})();